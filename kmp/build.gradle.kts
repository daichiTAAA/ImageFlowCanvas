// Root build configuration (placeholder, align with project build if needed)
plugins {
    // id("org.jetbrains.kotlin.multiplatform") version "<pin-in-repo>" apply false
}

// Configure repositories and pluginManagement at the root if needed.

